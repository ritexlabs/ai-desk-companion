fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.1
httpx==0.27.2

# ── System metrics agent ──────────────────────────────────────────────────────
psutil>=5.9

# ── Stock Market agent ────────────────────────────────────────────────────────
yfinance>=0.2.0
pandas>=2.0
numpy>=1.24

# ── Phase 5: wake-word detection (optional) ─────────────────────────
# Uncomment and install to enable server-side always-on wake-word:
#   pip install sounddevice openwakeword numpy
#   macOS only:  brew install portaudio
# sounddevice==0.5.1
# openwakeword==0.6.0
# numpy>=1.24
