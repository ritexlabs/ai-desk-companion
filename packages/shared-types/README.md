# shared-types

Future shared contract package for TypeScript schemas and DTOs used across the desktop UI and any Node-based tooling.

For now, the primary runtime contracts live in:
- `apps/desktop/src/types/runtime.ts`
- `apps/orchestrator/app/models/contracts.py`
